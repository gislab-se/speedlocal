# Daily plan — 2026-08-06

## Testable outcome

Restore the clean-process V2 Final localhost review from a stable, verified
Trøndelag runtime package and prove that all canonical population ranking
layers are ready before continuous wind/solar allocation runs.

## Tasks

- [x] Reproduce and trace the `canonical population ranking layers are not
  ready` failure from the active localhost process.
- [x] Verify the configured archive and generated-runtime roots, including the
  three population ranking sources and their declared checksums.
- [x] Restart localhost from a stable verified runtime root without changing
  promoted population or combined-result calculations.
- [x] Prove HTTP health, clean server logs, and readiness of
  `population_points`, `built_centre`, and `built_low_selection` in the actual
  Streamlit flow.
- [x] Record validation and the exact next visual-review checkpoint.

## Initial evidence

- Branch: `v2-final-dev`; the screenshot failure occurs during continuous
  wind allocation after the map calculation has begun.
- Failing boundary:
  `_allocation_priority_layer_groups()` rejects all three manifest-declared
  canonical population ranking layers as unavailable.
- Working hypothesis: the running process points at a transient extracted
  runtime directory from an earlier validator run. This must be tested before
  any business-logic change is considered.

## Localhost runtime-root repair

- Root cause confirmed: the failed worker had runtime-provider configuration
  drift. With either generated root absent or pointed at the complete external
  archive, the three generated population R7 distance tables are correctly
  marked unavailable. With both providers pointed at the verified runtime
  package, all population, nature, culture, and grid-infrastructure layers are
  ready. This is not a population-calculation defect.
- Materialized the checksum-pinned 48-file
  `speedlocal-v2-final-runtime-trondelag-r7-2026-08-04.1` package through the
  public runtime-bundle verifier into the dedicated localhost cache under
  `%TEMP%/speedlocal-v2-final/runtime-cache/797eb8fda67571825185/data`.
- Restarted port 8502 through the public repository `app.py` entrypoint with
  both providers pinned to that root and archive SHA-256
  `797eb8fda675718251850deac23cd7e2129ea25be835997d4d37043d7b0b3781`.
  Bootstrap therefore verifies the package before the V2 Final app runs.
- HTTP probe: 200. The connected browser session completed its rerun with no
  `RuntimeBundleError`, `ValueError`, traceback, or uncaught app execution in
  the new server log.
- Focused real Streamlit `AppTest` against root `app.py`: process exit 0;
  exceptions `[]`, errors `[]`, bootstrap technical codes `[]`.
- A redundant full `validate_v2_port_app.py` rerun exceeded the 300-second
  command limit before emitting its buffered final report. It showed no new
  blocker before termination; the same code checkpoint had already passed
  83/83 before this runtime-only restart, so this timeout is recorded as
  inconclusive rather than a pass.

Exact next visual checkpoint: refresh `http://127.0.0.1:8502/?region=trondelag`
and confirm the combined map renders past 6/6 calculation steps. Then resume
the pending visual review of the three independently committed corrections.

## Solar reset callback repair

- Localhost review reproduced a `StreamlitAPIException` when **Nollställ
  solfilter** assigned `solar_draft_small_population_active` after its keyed
  checkbox had already been instantiated during the same script run.
- The solar reset now follows the already accepted wind pattern: the button
  invokes `_reset_solar_filter_state` through `on_click`, so all applied and
  draft widget values are cleared before the rerun instantiates any keyed
  widget. The callback also invalidates the workspace cache.
- Added a real Streamlit regression that clicks the rendered reset button and
  requires an empty exception/error set, disabled rooftop draft, the
  manifest-default 10 m²/person slider value, inactive large-scale filter
  groups, and empty source/buffer review selections.
- Validation: Python compilation PASS; focused reset AppTest PASS; focused
  solar/rooftop real-app suite 6/6; full V2 Final app validator PASS with no
  blockers; V2 port guardrails 17/17; delivery repository 82/82.
- Classification: presentation/session-state repair only. No manifest,
  provider, geometry, population, rooftop-accounting, or potential-area
  calculation changed.

Exact next visual checkpoint: refresh localhost, activate one or more solar
filters, press **Nollställ solfilter**, and confirm that the controls return to
their defaults without a red error panel.

## Small-positive potential display

- Detailed combined-result popups no longer render a positive technology area
  as literal zero merely because it falls below the selected display precision.
  A positive score below one decimal percentage point is shown as `<0.1%`, and
  a positive area below three decimal km² is shown as `<0.001 km²`.
- Exact zero remains `0.0%` and `0.000 km²`. The compact integer hover,
  technology suitability, establishment class, geometry, capacity table, and
  scenario allocation are unchanged. No minimum suitability threshold was
  introduced.
- Added frozen-parity regression evidence for both tiny wind-only and tiny
  solar-only popup values, including a separate exact-zero assertion.
- Validation: Python compilation PASS; frozen-V2 baseline parity 143/143;
  V2 port guardrails 17/17; full real Streamlit app validator 84/84. No
  blockers.
- Classification: presentation-only correction. No manifest, provider,
  geometry, denominator, potential-area, or allocation calculation changed.

Exact next visual checkpoint: start localhost on port 8502, inspect either of
the two reviewed coastal cells, and confirm the detailed popup displays
`<0.1%` and `<0.001 km²` for its small positive technology while a genuinely
zero technology still displays literal zero.

## Continuous-mix persistence across solar controls

- Localhost review found that applying a solar layer returned the continuous
  energy mix to its 50% solar default. The solar form intentionally interrupts
  the current run with `st.rerun()` before the mix slider farther down the page
  is rendered, so Streamlit discarded the non-persistent widget state.
- The keyed mix slider now declares session persistence. Its selected value is
  therefore retained while the widget is temporarily absent during the solar
  form rerun; no separate or duplicated mix state was introduced.
- Added a real Streamlit cross-control regression: set 80% solar, activate and
  apply the protected-nature solar filter, then reset the solar filters. Both
  operations must preserve the 20% wind / 80% solar calculation and rendered
  summary.
- Validation: Python compilation PASS; focused real-app mix suite 2/2; full V2
  Final app validator 85/85; V2 port guardrails 17/17. No blockers.
- Classification: Streamlit session-state repair only. No manifest, energy,
  potential-area, geometry, filter, or allocation calculation changed.

Exact next visual checkpoint: choose a clearly non-default mix such as 80%
solar, apply one or more solar filters, then press **Nollställ solfilter** and
confirm that the mix remains 80% after both reruns.

## Eligible-surface slice

### Testable outcome

Wind and large-scale solar use one manifest-declared Trøndelag eligible land
surface so that water and area outside the regional mask cannot contribute to
potential area, percentage, map geometry, result tables, or continuous
allocation.

### Tasks

- [x] Add and validate a provider-resolved eligible-surface contract without a
  region-name algorithm branch.
- [x] Clip the canonical R7 analysis geometry and denominator to that surface,
  derive R6/R5 from R7, and cache the static normalization work.
- [x] Feed the same eligible geometry and area through the combined map,
  hover, tables, and allocation while preserving the five promoted group
  calculations.
- [x] Add independent coastal, manifest-failure, cross-consumer, and runtime
  regressions.
- [x] Update the authoritative program and delivery documentation and prepare
  a clean localhost review checkpoint.

### Acceptance checks

- Trøndelag's current onshore wind and large-scale land-solar contracts exclude
  sea and geometry outside the declared regional surface; inland-water policy
  remains explicit rather than implicit.
- One canonical eligible-area value is used by engine result, map geometry,
  hover, result table, and scenario allocation at R7, with R6/R5 derived from
  the clipped R7 children.
- Missing, invalid, unsupported, or checksum-drifting eligible-surface sources
  fail closed.
- The five locally promoted group regression metrics remain unchanged.
- Generic engine, baseline parity/drift evidence, vector preview, real app,
  runtime bundle, guardrail, and delivery gates pass before localhost review.

### Known risks

- The available Trøndelag mask excludes `Havflate` but intentionally retains
  inland water; treating all water classes requires a later, separately
  reviewed source contract.
- Correcting the denominator intentionally changes combined potential totals
  and continuous allocation near coasts and the county boundary, so frozen-V2
  numeric parity must be recorded as accepted product correction rather than
  silently preserved.
- Runtime clipping must not add a multi-second cost to every Streamlit rerun;
  static clipped geometry must be precomputed in the runtime package or
  bounded by a deterministic cache.

### Starting point

Open `speedlocal/contracts.py`, `speedlocal/area_result.py`, the Trøndelag
analysis manifests, and the runtime-package inventory. Define the data
contract before changing the Streamlit renderer.

### Implementation and evidence

- Added the manifest-declared `onshore_land` eligible surface shared by wind
  and large-scale land solar. The algorithm branches on declared surface
  scope, water policy, polygon geometry, and resolution, never region id.
- The deterministic build clips canonical R7 cells to the checksum-pinned
  Trøndelag land/region mask in EPSG:25832 and writes runtime-only R7/R6/R5
  geometry. R6/R5 areas are exact sums of the R7 children.
- Eligible area is 41,826.93063562673 km² at every declared resolution. The
  former full-cell domain is 45,213.18864360976 km², so 3,386.25800798308 km²
  of sea/outside-region cell area is removed. The current source intentionally
  retains inland water.
- Combined map geometry, hover, capacity table, acceptance capacity, and
  allocation use the same eligible geometry and area. Canonical source/buffer
  review and all five promoted group metrics remain unchanged.
- Prepared and pinned the local, unpublished 53-file runtime candidate
  `2026-08-06.1`: 118,149,644 expanded bytes, 19,145,804 ZIP bytes, SHA-256
  `5e0f83b65e74c1a58c40c72d3b7840391a48e9def7207ce1e02f908c43ae74fa`.
- Validation PASS: `validate_eligible_surface.py`; generic engine 6691/6691;
  accepted baseline 139/139; vector preview 39/39; V2 port app 85/85;
  runtime bundle 25/25; runtime sources 32/32; V2 guardrails 17/17; Python
  compilation and `git diff --check`. Delivery-repository checks pass 82 items
  and retain one expected tracking blocker until the new runtime contract is
  included in the post-review checkpoint commit.
- Classification: extract/configure/rewrite. Eligible-surface contracts,
  resolution and checksum validation, and provider resolution belong in
  `speedlocal/`; surface paths and policies belong in region manifests; the
  combined renderer now consumes preserved source geometry instead of
  reconstructing full H3 cells. Existing group engine and preview behavior is
  kept.
- No calculation, app, geometry, or package blocker remains. Local promotion
  and the tracking/checkpoint gate are deliberately pending clean localhost
  visual review.

Exact next visual checkpoint: open
`http://127.0.0.1:8502/?region=trondelag`, zoom to a reviewed coast or island,
and confirm that combined potential polygons stop at the actual land/region
surface instead of filling the complete H3 cell. Check at least one partial
coastal cell with the info tool and compare its wind/solar percentage with the
table after changing one filter. Also confirm that inland lakes remain in the
current candidate, as documented.

## End-of-day closeout — eligible-surface candidate

- Testable outcome: automated candidate PASS. Onshore wind and large-scale
  land solar now share one manifest-declared eligible surface at R7/R6/R5, and
  all calculation, map, hover, table, allocation, provider, and runtime gates
  use the same clipped area. Local promotion remains deliberately pending the
  coastal localhost review described above.
- Completed work: defined the eligible-surface contract and documentation;
  generated checksum-pinned R7/R6/R5 surface artifacts; integrated the
  provider-resolved geometry and denominator throughout the combined result;
  added fail-closed, coastal, cross-consumer, and runtime regressions; and
  prepared the unpublished 53-file Trøndelag runtime candidate `2026-08-06.1`.
- Final validation: eligible-surface gate PASS; generic engine 6,691/6,691;
  accepted baseline 139/139; vector preview 39/39; V2 port app 85/85; runtime
  bundle 25/25; runtime sources 32/32; V2 guardrails 17/17; post-commit
  delivery repository 83/83; Python compilation and `git diff --check` PASS.
- Runtime smoke review: the verified 53-file package passed explicit-root
  bootstrap and HTTP 200 on port 8502. Two launch-only bootstrap failures were
  traced to split provider roots and then a mistyped checksum; both were
  corrected without a code or calculation change. The final process used one
  verified root and the exact archive SHA-256, with one listener and no
  bootstrap error in its log.
- Blocker for local promotion: the user ended the workday after runtime startup
  was repaired but before visually reviewing the clipped coastal geometry.
  This does not block the validated development checkpoint and is not
  publication approval.
- Code classification: **keep** the five promoted group calculations and
  exact combined-result consumers; **extract** reusable eligible-surface
  contracts and resolution logic into `speedlocal/`; **configure** source,
  checksum, scope, and water policy in manifests; **rewrite** full-cell
  geometry reconstruction to consume preserved clipped geometry; **remove**
  implicit sea/outside-region area from the wind and land-solar denominator.
- Exact next starting point: create `docs/daily/2026-08-07.md`, start localhost
  from runtime candidate `2026-08-06.1`, and inspect at least one partial coast
  or island cell. Confirm clipped polygon boundaries, hover percentages, table
  consistency after a filter change, and the explicitly retained inland-water
  policy. If approved, record local promotion before selecting the next
  wind/solar slice.
- Publication: not used. `main` and the external V2 Final deployment remain
  unchanged. The runtime ZIP remains outside Git and unpublished.
- Localhost closeout: port 8502 has no remaining listener.
- Work-checkpoint commit: `0435009`
  (`Define manifest-driven eligible surface`). This is a validated development
  checkpoint, not local promotion or publication.
