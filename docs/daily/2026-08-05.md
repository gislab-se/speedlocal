# Daily plan — 2026-08-05

## Testable outcome

Complete the deferred clean-process localhost review of continuous wind/solar
allocation at 0%, 50%, and 100% solar, and either record visual approval for
local promotion or capture a reproducible defect without changing the five
promoted standard-group behaviors.

## Tasks

- [x] Confirm the `v2-final-dev` checkpoint and required Trøndelag runtime
  roots before launch.
- [x] Start the V2 Final Streamlit entrypoint on localhost port 8502 and verify
  HTTP health with no startup error.
- [ ] Review 0/100, 50/50, and 100/0 wind/solar allocation: constant total TWh,
  correct zero-demand rows, changing allocation, and stable potential classes.
- [x] Diagnose and repair intermittent map hover/click loss by declaring the
  combined potential as the sole primary interaction result and making every
  other visible overlay mouse-passive.
- [ ] Record approval or a reproducible blocker; update promotion and delivery
  documentation only if the visual gate passes.

## Launch evidence

- Branch: `v2-final-dev`, synchronized with `origin/v2-final-dev` before the
  daily-plan file was created.
- Entrypoint: `apps/v2_port/potential_app.py`.
- Runtime roots: complete read-only V2 archive plus the verified generated
  Trøndelag R7 runtime package from 2026-08-04.
- Localhost: HTTP 200 at `http://127.0.0.1:8502/?region=trondelag`; launcher
  PID 19564 and Streamlit worker PID 18780.

## Primary-result interaction candidate

- Root cause: every visible Leaflet layer had its own full-map Canvas renderer,
  so a sparse higher-z overlay could intercept hover/click before the combined
  potential layer received the event.
- The layered map now accepts an explicit `interaction_role`; only
  `primary_result` receives tooltip/popup events in inspect mode. All source,
  buffer, scenario-allocation, and schematic overlays remain visible but are
  mouse-passive. Pan mode remains the default and closes open map information.
- Validation: Python compilation and diff checks pass; generic engine
  6,655/6,655; V2 Final baseline 140/140; vector-buffer preview 29/29; real V2
  Final Streamlit AppTest 78/78; V2 port guardrails 17/17; delivery repository
  82/82. Localhost remains HTTP 200 on port 8502 with worker PID 18780.
- Localhost visual review was approved by the user on 2026-08-05. Hover and
  click reliably target the combined potential while the pan control remains
  usable, so this focused interaction repair is locally promoted.

## Continuation plan — three reported behavior defects

### Testable outcome

Deliver three independently reviewable, manifest-driven corrections: solar
map-review controls expose every applicable preview source; grid proximity uses
the same exact metric geometry in preview and potential-area calculation; and
small-scale rooftop solar reduces the residual ground-mounted solar demand
without changing the geographic large-scale potential surface.

### Tasks

- [x] Characterize all three current paths from manifest through UI, analysis,
  map, hover, and result table, including accepted behavior and intended drift.
- [x] Correct and commit manifest-driven solar source/buffer map review.
- [x] Correct and commit the grid map-review preview so it exposes the exact
  domain-clipped metric geometry already used by public R7 potential; prove
  the existing R7→R6/R5 exact-area result remains unchanged.
- [x] Correct rooftop-energy accounting against residual
  ground-mounted solar demand using validated manifest parameters.
- [ ] Run the applicable full regression set, restart the clean localhost
  process after module-boundary changes, and request visual approval.

### Acceptance checks

- No region id, hardcoded layer list, or hidden conversion default selects new
  behavior; missing or unsupported contracts fail closed with a visible reason.
- Map review remains presentation-only, and the grid source, metric proximity
  geometry, per-hex percentages, hover, and result table agree.
- Zero rooftop selection reproduces the current result; increasing rooftop
  contribution monotonically reduces only residual ground-solar demand and
  allocation, while total scenario energy and geographic potential stay fixed.
- Required generic-engine, accepted-reference, vector-preview, real-app,
  runtime, guardrail, and delivery-repository validators pass as applicable.

### Known risks

- The promoted five-group grid metric deliberately remains its soft-distance
  diagnostic contract. Only its obsolete whole-cell map preview is replaced;
  the already promoted exact wind/solar area must remain numerically unchanged
  and be checked against the corrected preview geometry.
- Rooftop input currently appears to be a display template. Energy conversion
  must be declared and validated before it can affect land-demand accounting.

### Grid-preview correction candidate

- Replaced the obsolete whole-R7-cell grid review path with an exact metric
  vector buffer clipped locally to the declared R7 analysis domain. The
  preview reuses the same validated provider, buffering, overlap-safe clipping,
  declared cell-area weighting, and large-feasibility dissolve optimization as
  the wind/solar technology-area result.
- Removed the replaced `analysis_cell_coverage` preview implementation. The
  promoted soft-distance group metric and every R7/R6/R5 group anchor remain
  unchanged.
- Fixed the grid popup to report model area inside the analysis domain while
  retaining the truthful pre-clip area label for the other four standard
  groups.
- Exact grid anchors pass at 500/2,000/15,000 m, including accepted
  zero/partial/full R7 counts. The 2,000 m wind and solar per-cell frames are
  identical, R7 total area survives R6/R5 rollup, and serialized map geometry
  round-trips to its clipped metric area.

Validation on the candidate:

- `scripts/validate_generic_engine.py`: PASS, 6,655/6,655.
- `scripts/validate_v2_final_baseline_parity.py`: PASS, 140/140.
- `scripts/validate_vector_buffer_preview.py`: PASS, 34/34.
- `scripts/validate_v2_port_app.py`: PASS, 78/78 after preserving the promoted
  soft-distance diagnostic metric.
- `scripts/validate_runtime_bundle.py`: PASS, 20/20.
- `scripts/validate_v2_port_guardrails.py`: PASS, 17/17.
- `scripts/validate_delivery_repo.py`: PASS, 82/82.

The correction is committed as an independently reviewable candidate; clean-
process localhost review is still required before local promotion.

Grid candidate commit: `e0f69f1` (`Use exact geometric grid review`).

### Solar map-review correction candidate

- Generalized the bridge around validated area analyses and added explicit
  manifest-driven applicable-group, layer, control, source, and exact preview
  functions without changing existing wind call signatures.
- Solar's five standard map-review groups now resolve against the `solar`
  manifest and shared provider resolver. Public visual ids remain stable only
  as session keys and map to canonical group ids at the boundary.
- Replaced registry/runtime-backed solar source and buffer display factories.
  Every large-scale buffer now shows exact domain-clipped R7 geometry; grid
  review matches wind at 2,000 m. The separate rooftop schematic review remains
  unchanged.
- Removed unused protected-source/buffer wrappers from the replaced map-review
  path. Legacy calculation adapters outside this candidate boundary remain for
  later solar/allocation slices.
- Confirmed a pre-existing, out-of-boundary form mismatch: solar slider limits
  still come from `SOLAR_FILTER_GROUP_SPECS` (for example roads 0–500 m versus
  manifest 100–2,000 m). This remains explicit work for the later solar-control
  slice; it does not affect the map-review geometry corrected here.

Validation on the candidate:

- `scripts/validate_generic_engine.py`: PASS, 6,655/6,655.
- `scripts/validate_v2_final_baseline_parity.py`: PASS, 140/140.
- `scripts/validate_vector_buffer_preview.py`: PASS, 39/39, including all five
  solar groups, exact solar analysis metadata, wrong-group rejection, and
  shared 2,000 m grid geometry.
- Focused real-app solar map review: PASS, 2/2.
- `scripts/validate_v2_port_app.py`: PASS, 80/80.
- `scripts/validate_runtime_bundle.py`: PASS, 20/20.
- `scripts/validate_v2_port_guardrails.py`: PASS, 17/17.
- `scripts/validate_delivery_repo.py`: PASS, 82/82.

The solar map-review correction is ready for its independent commit. Clean-
process localhost review remains required before local promotion.

Solar map-review candidate commit: `2cc3329` (`Drive solar map review from
manifests`).

### Rooftop-energy accounting candidate

- Added a typed `distributed_generation.rooftop_solar` contract to the
  Trøndelag solar manifest. It pins the population CSV and checksum, direct
  R8→R7 aggregation, R6/R5 rollups, panel-area slider, PVGIS planning yield,
  target cap, the canonical population source/default buffer used for map
  review, and the rule that rooftop output creates no land candidates.
- The source total is 477,978 people. The schematic clips at canonical R7 and
  consistently maps 477,755 at R7/R6/R5; 223 outside-domain people remain in
  the explicitly declared source-total energy-accounting policy.
- At 10 m²/person the proxy contributes 0.801326 TWh. Gross solar energy stays
  fixed, while residual ground-solar TWh and its declared km²/TWh land demand
  decrease. Exact geographic potential, hover, acceptance capacity, landscape
  distribution, and ground candidate semantics remain unchanged.
- Removed the regional-pipeline/Bornholm fallback and the legacy path that
  treated rooftop schematic area as ground-establishment potential. Removed
  the rooftop-only registry/hardcoded-100-m map adapter; source and exact
  buffer review now resolve from the validated solar manifests.
- The six-site PVGIS mean remains explicitly classified as an accepted
  planning assumption until its six individual queries/results are pinned as
  a small provenance artifact.

Validation so far on the candidate:

- `scripts/validate_generic_engine.py`: PASS, 6,691/6,691.
- `scripts/validate_v2_final_baseline_parity.py`: PASS, 142/142.
- `scripts/validate_vector_buffer_preview.py`: PASS, 39/39.
- Focused real-app solar review and rooftop accounting: PASS, 5/5.
- `scripts/validate_v2_port_app.py`: PASS, 83/83.
- `scripts/validate_runtime_bundle.py`: PASS, 20/20.
- `scripts/validate_v2_port_guardrails.py`: PASS, 17/17.
- `scripts/validate_delivery_repo.py`: PASS, 82/82.
- `scripts/validate_frozen_v2_reference.py`: PASS, 50/50 (local archive;
  remote verification remains a publication-window gate).
- Python compilation and diff checks: PASS.

The clean-process restart and localhost visual review remain before local
promotion.
