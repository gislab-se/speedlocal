# Daily plan — 2026-07-29

## Outcome

Select V2 Final as the authoritative strategy and establish the controlled
slice-by-slice delivery process.

## Starting state

- Branch and commit: `main`, `b357e17`.
- Active slice: roads.
- Previous handoff: generic `roads_large` execution works for Bornholm and
  Trøndelag but is not connected to the public Streamlit flow.

## Tasks

- [x] Compare V2 Final with alternative architectures.
- [x] Select V2 Final as the project strategy.
- [x] Rewrite the authoritative plan.
- [x] Create the dated delivery schedule.
- [x] Establish the daily planning and handoff routine.
- [x] Characterize the complete V2 road calculation and controls.
- [x] Add dynamic medium and large road contracts for both active regions.
- [x] Implement generic group composition with V2 parity.
- [x] Add a temporary Streamlit parity surface behind a feature flag.

## Acceptance checks

- One document is authoritative for architecture.
- One dated plan leads from today to acceptance review.
- Future agents receive the same daily workflow through `AGENTS.md`.
- Current roads status and next starting point are explicit.

## Decisions and discoveries

- V2 will be dismantled by complete behavior slice, not cleaned mechanically
  function by function.
- A controlled rewrite is allowed inside each slice.
- Existing V2 behavior remains the parity reference until replacement.
- Regional extras are deferred until the five common groups work.
- V2 blocks road cells at `distance <= buffer`, not only below the buffer.
- Bornholm's medium-road distance artifact is valid, but its source GeoJSON
  contains null geometry. Analysis is allowed through an explicit
  `precomputed_distance` contract; source-line display remains unavailable.

## Code classification

- Keep: `_group_distance_frame` and `_distance_conflict_acceptance` as behavior
  references until road promotion.
- Extract: minimum-distance composition, intersection composition, blocking,
  and soft acceptance are now in `speedlocal/engine.py`.
- Configure: road sources, labels, geometry representation, and buffer bounds
  now live in region wind manifests.
- Rewrite: the public road section of `_wind_group_controls` still needs to
  become a thin request/render path.
- Remove: road allowlist and duplicate road selectors only after promotion.

## Completed

- Created the V2 Final architecture plan.
- Created the delivery plan through the target acceptance review.
- Created the daily workflow and first daily log.
- Added complete generic road contracts for Bornholm and Trøndelag.
- Added combined group results matching V2 semantics.
- Added `apps/generic_parity/app.py`, enabled only by
  `SPEEDLOCAL_GENERIC_ROADS_PARITY=1`.
- Documented the road slice in `docs/slices/roads.md`.

## Validation

- `scripts/validate_delivery_repo.py`: PASS, 56 checks and no blockers.
- `git diff --check`: PASS after removing one Markdown trailing-space marker.
- `scripts/validate_generic_engine.py`: PASS, 25/25 including two-region V2
  road parity.
- `scripts/validate_generic_roads_app.py`: PASS, 10/10.
- `scripts/validate_v2_port_guardrails.py`: PASS, 17/17.
- `scripts/validate_v2_port_app.py`: PASS, 10/10.
- `scripts/validate_region_readiness.py`: PASS, 34/34.

## Blockers

- Skaraborg runtime data readiness must be confirmed before its onboarding
  phase.

## Next starting point

Run the full V2 smoke and guardrail regressions. Then design the smallest
promotion adapter that lets the existing public road controls build a generic
`run_analysis` request without introducing product features in
`apps/v2_port/`. Remove old road code only after that public path passes.

## Commit

- Not committed.
