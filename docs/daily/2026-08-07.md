# Daily plan — 2026-08-07

## Outcome

Locally promote the reviewed Trøndelag eligible-surface correction and define
the next testable continuous wind/solar allocation boundary from the current
manifest, energy-demand, and allocation paths without changing the five
promoted standard-group behaviors.

## Starting state

- Branch and commit: `v2-final-dev` at `39fc97e`; clean worktree and aligned
  with `origin/v2-final-dev` at start of day.
- Active slice: wind and solar, with the eligible-surface correction awaiting
  only localhost visual approval.
- Previous handoff: inspect clipped coastal/island geometry, hover/table
  consistency after a filter change, and the explicitly retained inland-water
  policy before local promotion.

## Tasks

- [x] Record the user's localhost approval and identify the land-clipping
  source, generated surfaces, and wind/solar references in the manifests.
- [x] Update authoritative status documentation from candidate to locally
  promoted and run the tracking/guardrail checks.
- [x] Trace the continuous mix from scenario energy through technology area
  demand and eligible-surface allocation, including exact 0/100 endpoints.
- [x] Define and validate the next bounded replacement boundary; do not reopen
  the five promoted standard groups.

## Acceptance checks

- The same checksum-pinned `onshore_land` surface remains the denominator and
  display/allocation geometry for both wind and large-scale land solar at
  R7/R6/R5.
- Sea and outside-region area stay excluded, inland water remains explicitly
  retained, and no region-name algorithm branch is introduced.
- Existing eligible-surface, baseline, real-app, guardrail, and delivery gates
  pass for the promotion/status change.
- The next allocation boundary has an explicit accepted behavior, replaced
  path, automated gate, and localhost review checkpoint before implementation.

## Decisions and discoveries

- The user approved the 2026-08-06 localhost visual candidate on 2026-08-07:
  the reviewed result looks correct.
- `onshore_land` is a dedicated manifest-declared eligible-surface contract,
  not a standard constraint layer. The manifest declares its source mask,
  checksum, policies, generated R7 geometry, and R6/R5 rollups; wind and solar
  reference it through `area_result.eligible_surface_id`.
- The next slice is the technology-neutral continuous allocation boundary, not
  replacement of Trøndelag's explicitly provisional scenario inputs. Its
  accepted contract is recorded in
  `docs/slices/wind_solar_allocation.md`.
- The existing flow places residual ground solar first and passes those ids to
  wind as a last-resort overlap signal. Any extracted engine must preserve the
  accepted co-use behavior while bounding both technologies by declared
  eligible area.

## Code classification

- Keep: the five promoted group calculations and exact combined-result
  consumers.
- Extract: total-preserving mix, technology demand, eligible-capacity
  selection, overlap policy, outside-potential classification, and R7 rollup
  into `speedlocal/` in the next slice.
- Configure: eligible surface source, checksum, policies, and technology
  applicability remain manifest-owned.
- Rewrite: wind and solar app adapters to consume one generic allocation
  result.
- Remove: duplicated app-local allocation calculations after parity and
  localhost approval.

## Completed

- Confirmed the clean development branch and reviewed the previous handoff.
- Confirmed the complete manifest and runtime path for `onshore_land`.
- Updated the authoritative plan, delivery status, and eligible-surface
  contract to record local promotion.
- Characterized the next continuous wind/solar allocation slice, including
  exact endpoint, eligible-capacity, co-use, shortage, rollup, and consumer
  gates.

## Continuation — generic allocation engine

### Testable outcome

Introduce a technology-neutral typed allocation result in `speedlocal/` and
prove exact mix endpoints, demand conservation, deterministic eligible-area
selection, and R7-derived rollup before changing the Streamlit call sites.

### Tasks

- [x] Define immutable mix, demand, candidate, selected-cell, and technology
  result types without pandas or Streamlit dependencies.
- [x] Implement fail-closed mix, demand, eligible-capacity selection, overlap,
  and parent-rollup operations.
- [x] Add an independent synthetic validator for 0/50/100 endpoints, partial
  coastal capacity, row-order determinism, overlap policy, invalid input, and
  R7-child conservation.
- [x] Integrate the validated pure model at the first V2 Final adapter boundary
  and remove the replaced duplicate calculation.
- [x] Run focused and applicable repository/app gates and record the exact next
  localhost checkpoint.

### Known risks

- Existing ranking contains accepted technology-specific fields; the generic
  engine must accept an already-computed priority without absorbing landscape
  or social-acceptance algorithms.
- Outside-potential display is a schematic shortage path, not geographic
  potential. It must stay explicitly classified and inside the eligible
  surface.
- The unpublished full runtime package is not currently materialized locally;
  pure gates must not be mistaken for complete app parity.

### Implementation and evidence

- Added `speedlocal/allocation.py` with immutable technology energy, demand,
  candidate, selected-cell, result, and rollup types. It has no pandas,
  Streamlit, region-id, or H3 dependency.
- Added generic total-preserving mix, explicit-factor demand, deterministic
  eligible-capacity allocation, and parent summation. Invalid shares, factors,
  duplicate ids, missing parents, non-finite values, and potential area above
  eligible area fail closed.
- The accepted overlap behavior is explicit: supplied priority wins first;
  reservation by the other technology is the final tie-break, so higher-ranked
  co-use remains possible and an equally ranked separate cell wins.
- Replaced duplicate mix and demand arithmetic in the existing V2 Final pandas
  adapter with calls to the generic engine. Candidate ranking and placement
  loops are intentionally unchanged in this first checkpoint.
- Rebuilt the three deterministic direct-R7 population tables and eligible
  surface into an untracked temporary overlay. The population checksums match
  the reviewed runtime values, allowing complete current-checkpoint gates.
- Validation PASS: allocation oracle 12/12; V2 Final adapter compatibility at
  0/50/100; Python compilation; eligible-surface gate; generic engine;
  accepted baseline; full real Streamlit app with no blockers; delivery
  repository 84/84; and `git diff --check` before documentation closeout.
- The Streamlit skill's organization guidance supports the chosen boundary:
  Streamlit retains session/UI and pandas adaptation while testable business
  logic moves to `speedlocal/`. No new deprecated Streamlit API was added.

### Exact next integration point

Adapt `allocate_wind_area_from_core_hexes` and `_solar_establishment_frame` to
construct `AllocationCandidate` values and consume one
`TechnologyAllocationResult`. Preserve their current priority columns and
returned presentation frames, then remove the replaced selection loops. Do not
touch outside-potential expansion until both inside-potential adapters pass
row-for-row parity.

## Continuation — inside-potential adapters

### Testable outcome

Wind and ground solar select inside-potential scenario area through
`speedlocal.allocation`, reproduce their previous ordered frames and summary
statistics, and remove both replaced selection loops without changing shortage
expansion or presentation behavior.

### Tasks

- [x] Capture independent legacy wind/solar frames and statistics for normal,
  partial-final-cell, zero-demand, reversed-row, and overlap cases.
- [x] Route wind candidate capacity and accepted priority fields through the
  generic typed engine; remove the old wind selection loop.
- [x] Route solar candidate capacity and accepted priority fields through the
  same engine; remove the old solar selection loop.
- [x] Prove adapter contract parity and run allocation, baseline, full
  Streamlit, guardrail, and delivery gates.
- [x] Start a clean localhost process and provide the short visual checklist if
  the complete review boundary is ready.

### Acceptance details

- Both adapters must cap positive potential by each row's declared eligible
  area, not theoretical H3 area.
- Existing technology-specific ranking stays outside the generic engine and is
  supplied only as an ordered priority tuple.
- The outside-potential expansion functions remain unchanged in this
  checkpoint.

### Continuation implementation and evidence

- Wind and ground solar now construct `AllocationCandidate` values and consume
  `TechnologyAllocationResult`; both replaced inside-potential selection loops
  are removed from the V2 Final adapters.
- Technology-specific landscape, technical, and social ranking remains in the
  adapters and enters the generic engine only as deterministic priority tuples.
- Wind preserves the accepted last-resort overlap behavior. A higher-ranked
  shared cell may still win, while an equally ranked unreserved cell wins the
  final tie.
- Solar now reports allocated cell share against each cell's declared eligible
  land area. This intentionally replaces the obsolete theoretical-H3
  denominator for partial coastal and boundary cells.
- The generic engine preserves every positive allocation, including a tiny
  final remainder, rather than discarding it through a display-scale tolerance.
- Focused validation PASS: allocation engine 13/13 and wind/solar adapters 7/7,
  including partial-final-cell, reversed-row, zero-demand, missing eligible
  area, and overlap cases.
- Complete gates PASS: accepted baseline 143/143; generic engine 6691/6691;
  eligible surface; V2 port guardrails 17/17; delivery repository 85/85; and
  real Streamlit app 85/85 in 674.1 seconds with no blockers.
- Python compilation and `git diff --check` PASS. Existing bare-mode Streamlit
  warnings are unchanged and outside this behavior boundary.
- The first localhost handoff used the reviewed source root plus a separate
  generated overlay. Streamlit health returned HTTP 200, but the public root
  entrypoint correctly failed closed with `explicit_source_root_incomplete`;
  the health endpoint alone did not exercise the user script.
- Rebuilt the exact 53-file runtime ZIP outside Git from those two reviewed
  roots. Its bytes and SHA-256 match the tracked contract, and the release-
  archive/runtime-bootstrap validator passes 25/25, including the real root
  Streamlit entrypoint and interactive V2 Final gate.
- Extracted the verified package below a short Windows temp prefix to avoid
  legacy path-length failures, configured both runtime roots to that one
  complete directory, and restarted `http://127.0.0.1:8502` in a clean
  process. Health and root HTTP checks return 200. Visual approval remains
  open.

## Validation

- V2 port guardrails: PASS, 17/17.
- Delivery repository: PASS, 83/83.
- `git diff --check`: PASS after removing one trailing-space defect.
- Focused continuous-allocation oracle: PASS. The 0/50/100-percent solar
  endpoints preserve 62.1 TWh, produce the expected 3,105/1,863/621 km² total
  technology demand under the synthetic factors, and create no phantom
  allocation for zero demand. A partial coastal candidate is capped at its
  declared 0.4 km² eligible/potential area.
- Focused eligible-surface rerun: deterministic R7/R6/R5 artifact build PASS;
  the full focused validator then stopped because the prior unpublished
  runtime package containing the three generated population tables is no
  longer present locally. The identical candidate gate passed before the
  reviewed checkpoint; no calculation or contract changed today.

## Blockers

- None.

## Next starting point

Open `apps/v2_port/apps/potential_model/energy_modeling.py`,
`apps/v2_port/potential_app.py`, and `speedlocal/contracts.py`. Define the
technology-neutral allocation result and its synthetic endpoint/capacity
oracle before changing Streamlit call sites.

## Work checkpoint

- Branch: `v2-final-dev`.
- Commit: `526df70` (`Extract continuous allocation engine`). The earlier
  eligible-surface promotion checkpoint is `1b85c6a`.
- Push: PASS to `origin/v2-final-dev`.
- GitHub development checkpoint: branch push confirmed by Git.

## Local promotion

- Status: locally promoted on 2026-08-07; unpublished.
- Scope: shared `onshore_land` eligible surface for onshore wind and
  large-scale land solar.
- Automated evidence: complete 2026-08-06 candidate suite passed at checkpoint
  `0435009`; current documentation/guardrail gates pass. The focused geometry
  artifacts rebuild deterministically; its full rerun requires rematerializing
  the unpublished runtime package's generated population tables.
- Localhost visual evidence: user approval received 2026-08-07 for the coastal
  clipping candidate handed off on 2026-08-06.
- Continuous-allocation engine status: first pure-engine and mix/demand adapter
  checkpoint validated but not locally promoted as a complete slice. Inside-
  potential solar/wind placement adapters remain on the documented old path.

## Publication

- Window: Friday publication window available but not requested or used.
- Emergency reason, if outside Tuesday or Friday: not applicable.
- Published commit on `main`: none.
- GitHub published checkpoint: none.
- V2 Final deployment: unchanged.
- Runtime release, if applicable: unpublished candidate `2026-08-06.1`.
- Accepted-reference comparison: local candidate evidence only.
- GitHub Pages, if applicable: not applicable.
- Publication-record commit: none.
- Final worktree: clean after the checkpoint-record commit.
