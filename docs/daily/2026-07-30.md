# Daily plan — 2026-07-30

## Outcome

Remove the separate parity product path, technically freeze V2, and restore an
interactive manifest/provider-backed baseline in the one active V2 Final
monolith. Correct regional reference scope so frozen V2 is authoritative for
Trøndelag only.

## Starting state

- Branch and commit: `main`, `41725b2`.
- Active slice: roads promotion.
- Previous handoff: generic road calculation has two-region numerical parity,
  but was exposed through a temporary separate Streamlit parity app. That
  wording is now reclassified as engine-contract conformance, not two-region
  public behavioral parity.

## Tasks

- [x] Consolidate the product path: remove the parity app, restore the single V2
  Final entrypoint, and align strategy, deployment, and publication docs.
- [x] Freeze verified V2 with locked remote refs, a protected tag, read-only
  local files, checksums, and a repeatable validator.
- [x] Repair the V2 Final baseline through the shared provider: remove duplicate
  registries/path logic, make runtime strategy manifest-driven, reproduce the
  declared road checkpoints, and fail closed on missing or unsafe state.
- [x] Trace the historical region decision, make Trøndelag the sole
  frozen-V2 parity authority, and move Bornholm to disabled onboarding.
- [x] Complete all automated regression checks and the user's localhost visual
  review before creating the work checkpoint or publishing.

## Acceptance checks

- No separate parity app or parity feature flag remains.
- Localhost always opens the actual V2 Final monolith.
- Frozen V2 and V2 Final have separate documented deployment roles.
- Daily work cannot be marked published until the external app is verified.
- Frozen remote refs and local runtime assets fail validation if their recorded
  identities change.
- A submitted road-buffer change affects the visible Trøndelag V2 Final result
  at declared frozen-public checkpoints.
- Bornholm stays visible but cannot open V2 Final until its V1/reference and
  regional acceptance gate pass.

## Decisions and discoveries

- V2 Final is not a clean replacement app. It is the working V2 monolith being
  reduced and made manifest-driven slice by slice.
- Generic modules are acceptable only when they replace monolith behavior and
  are called from the existing V2 Final UI.
- GitHub Pages and the interactive Streamlit deployment are separate
  publication systems.
- The repository contains no Streamlit deployment workflow. The Streamlit
  Cloud repo/branch/entrypoint connection and cloud runtime provider must be
  verified externally before publication can be claimed.
- Port `8502` is the single documented V2 Final localhost port.
- Skaraborg remains visible as planned but has no V2 Final link until its
  manifest and runtime data pass readiness.
- Frozen V2 was verified as
  `gislab-se/landskapsanalys@75ba14871100c208cbf8eedb794d56c165340811`
  with entrypoint `streamlit_app.py`.
- Branch `frozen-v2-reference-2026-07-30` and tag
  `v2-frozen-reference-2026-07-30` were created at that commit.
- Both the dedicated frozen branch and the Streamlit deployment's existing
  `potential-v2-multiregion` branch are protected with administrator
  enforcement, force pushes and deletion disabled, and branch locking enabled.
- Tag `v2-frozen-reference-2026-07-30` is protected against updates and
  deletion by active GitHub ruleset `20038506`.
- The local V2 folder has a broken Git worktree pointer and two known local
  additions. It is a read-only runtime archive, not Git provenance.
- Public Streamlit metadata confirms app id
  `63561ff3-f8c2-4c09-a9e7-ea110c51dc4a`, Python 3.11, and the original source
  branch. Because that source branch is now locked at the frozen commit,
  repointing to the dedicated frozen branch is optional clarity work rather
  than a freeze blocker.
- The first generalized Bornholm distance approximation produced 0.4%, while a
  later multiregion archive fixture reports 3.9%. The approximation was
  rejected rather than documented as parity.
- Trøndelag's frozen public path is the soft H3 distance runtime. Bornholm's
  archive diagnostic uses a buffered polygon runtime with partial-area H3
  display. Runtime strategy is now source-manifest data rather than a
  region-name conditional.
- This computer has no R/geospatial polygon runtime. Bornholm therefore loads
  only two fully checksummed read-only diagnostic fixtures for now: roads at
  300 m and 400 m. Other combinations return no number and a clear note.
- Behavioral widget state is intentionally reset to validated defaults whenever
  the active region changes. Language and layout preferences are preserved.

## Baseline-scope correction

- Git history contains commit
  `9095d996797a3173e7bbc5315a0beae5f712011e`, `Make v2 Trondelag only`.
- The older Trøndelag-only history checkpoint is
  `stable-v2-trondelag-2026-05-19-zoom` at
  `856289015c31f31bf09e9df226336a49cc19dbfd`.
- That older tag is a secondary historical control, not the sole numerical
  oracle. The immutable snapshot at `75ba148` contains later accepted
  Trøndelag behavior and remains the current full-flow parity reference.
- Bornholm's package in the later multiregion snapshot was imported from V1.
  Its V2 archive fixtures therefore remain provenance/diagnostic evidence, not
  an accepted V2 behavioral baseline.
- No reset was performed. The immutable multiregion snapshot remains intact,
  while SpeedLocal moves forward with a corrected reference policy.
- Trøndelag is active and authoritative for frozen-V2 parity.
- Bornholm is indexed as `onboarding`, its V2 Final route is disabled, and V1
  remains the intended visual reference until technically pinned.
- Skaraborg remains planned.

## Code classification

- Keep: root `app.py` as launcher for the active V2 Final monolith.
- Extract: generic road computation remains under `speedlocal/`.
- Configure: road catalogs and parameters remain in region manifests.
- Configure: external Streamlit source settings and a cloud-accessible runtime
  provider remain deployment work outside the local Windows fallback.
- Rewrite: actual road controls and road execution path inside V2 Final.
- Remove: temporary `apps/generic_parity/`, its validator, and feature flag.

## Completed

- Separate parity app removed.
- Single V2 Final entrypoint restored.
- Strategy, deployment, delivery, slice, and daily workflow docs aligned.
- Active README and repo-hygiene guidance now distinguish frozen V2 from the
  editable V2 Final monolith.
- Landing-page buttons now name V2 Final and frozen references explicitly,
  route localhost to port `8502`, and keep Skaraborg disabled.
- Local Streamlit secrets are protected by `.gitignore`.
- Frozen V2 identity is machine-readable and guarded by a checksum validator;
  its exact remote branch and tag were created at the verified commit.
- The V2 Final baseline now resolves external acceptance registries and all
  their runtime assets through the shared `v2_archive` provider.
- Duplicate local acceptance registries and duplicate repo/port path-resolution
  code were removed.
- Runtime dispatch is manifest-driven: Trøndelag uses `fast_distance`;
  Bornholm's diagnostic fixture loader uses `precomputed_polygon`.
- Bornholm's validated fixtures are located by effective configuration and
  verified by complete per-file SHA-256 inventories before loading.
- Every fixture directory and file is resolved again at read time and must stay
  inside its declared artifact root, including through symlinks or junctions.
- Missing `SPEEDLOCAL_V2_SOURCE_ROOT` renders one actionable app error instead
  of an uncaught exception or silently disabled controls.
- Direct query-parameter region switches clear behavioral state and remove
  unavailable ghost layers.
- App tests exercise the actual Trøndelag road slider and submitted result.
  Bornholm's direct route is tested as disabled; its archive fixtures run only
  in the separate diagnostic validator.

## Validation before baseline repair

- `scripts/validate_delivery_repo.py`: PASS after updating its entrypoint
  assertion for the V2 Final name, 56/56.
- `scripts/validate_generic_engine.py`: PASS, 25/25.
- `scripts/validate_v2_port_guardrails.py`: PASS, 17/17.
- `scripts/validate_v2_port_app.py`: PASS, 10/10.
- `scripts/validate_static_site.py`: PASS, 10/10.
- `scripts/validate_region_readiness.py`: PASS, 34/34.
- `scripts/validate_file_runtime_summary.py`: PASS, 23/23.
- `scripts/validate_trondelag_runtime_sources.py`: PASS, 32/32.
- V2 Final localhost health check: PASS at `http://127.0.0.1:8502`.
- Visual localhost review: awaiting user review.

## Validation after baseline repair, before reference-scope correction

- `scripts/validate_frozen_v2_reference.py --remote`: PASS, including both
  frozen remote refs, all read-only archive files, ten core checksums, and the
  aggregate checksum for 120 declared acceptance runtime assets.
- `scripts/validate_v2_source_adapter.py`: PASS, 31/31, including provider
  confinement, 14/14 Trøndelag readiness, and 26/27 Bornholm readiness.
- `scripts/validate_v2_final_baseline_parity.py`: PASS, 8/8. Bornholm's
  checksummed polygon fixtures reproduce 3.9% at 300 m and 3.3% at 400 m with
  partial-area H3 cells; Trøndelag reproduces the frozen raw fast-distance
  values at 300 m and 1000 m. Artifact-path confinement also rejects direct
  and symlink escapes.
- `scripts/validate_v2_port_app.py`: PASS, 27/27. The real UI reproduces
  Trøndelag 6.7% to 6.2% at 300 to 1000 m and Bornholm 3.9% to 3.3% at 300 to
  400 m; unknown Bornholm combinations and missing source configuration fail
  closed; query-driven region switches do not leak state.
- Full final regression: delivery 60/60, static site 10/10, region readiness
  34/34, Trøndelag runtime sources 32/32, file-runtime summary 23/23, V2
  guardrails 17/17, source adapter 31/31, and generic distance-engine contract
  25/25.

The Bornholm fixture results above remain valid archive-integrity evidence but
are no longer classified as parity or product readiness. Corrected validators
split Trøndelag parity from Bornholm diagnostics.

## `roads_large` R7 canonical checkpoint

The first visible V2 Final slice now uses the canonical engine:

- `roads_large` at R7 is executed by `speedlocal.run_analysis`;
- its per-cell results are restricted to the 13,735 cells in the public
  Trøndelag R7 display domain;
- that domain is declared in the canonical analysis manifest and must match
  the display-cell ids exactly;
- the 116 valid distance rows outside that domain are deliberately excluded;
- the existing `roads_medium` and all other groups remain on the legacy path
  and are combined with the canonical large-road acceptance without changing
  the full-flow result;
- the road slider reads default/range/step from the canonical manifest:
  300/100/2000/25 m;
- failures in the canonical R7 calculation stop the result instead of silently
  falling back to legacy `roads_large`;
- per-cell `hex_id → acceptance` values are compared with an independent
  frozen-V2 oracle, and malformed distance rows fail closed;
- R6/R5 still use legacy rollups, so this is explicitly not full roads
  promotion and no shared legacy road code has been deleted.

Measured isolated `roads_large` parity:

- 300 m: 13,735 cells, 428 fully blocked, 96.8838733163451% remaining;
- 1000 m: 13,735 cells, 434 fully blocked, 95.54751146705496% remaining.

Validation after the checkpoint:

- generic engine: PASS, 56/56;
- Trøndelag frozen-V2 baseline and R7 slice parity: PASS, 6/6;
- real Streamlit AppTest: PASS, 20/20, including the canonical slider
  contract, full default flow at 6.7%/6.2%, and isolated `roads_large` at
  96.9%/95.5%;
- compileall and `git diff --check`: PASS;
- V2 Final and the historical Trøndelag-only checkpoint both answer HTTP 200
  on localhost ports 8502 and 8503.

### Live-server correction

The first browser review exposed
`TypeError: load_registry() takes 0 positional arguments but 1 was given`.
Four Streamlit worker processes were listening concurrently on port 8502, and
the browser was still connected to the oldest process from before the provider
refactor. Stopping only the visible virtual-environment launcher had not
stopped its real Python worker.

All four verified 8502 listeners were stopped and one clean V2 Final instance
was started from the current working tree with
`SPEEDLOCAL_V2_SOURCE_ROOT` configured. Port inspection now shows exactly one
listener. The replacement process answers HTTP 200, and the current
clean-process Streamlit test passes 20/20, including an applied road-buffer
change from 300 m to 1000 m. `scripts/start_app.ps1` now refuses to create a
duplicate listener, and the daily workflow requires a complete process-tree
restart after imported function signatures or module boundaries change.
The user then reviewed the clean localhost instance and approved the visible
V2 Final result for publication.

## Cloud runtime repair

The first external V2 Final review reached the current root app but stopped
because Streamlit Cloud had no local
`C:\gislab\data\landskapsanalys-v2-multiregion` directory. The repair keeps the
existing `v2_archive` provider semantics and materializes its root before the
monolith is imported.

Findings and decisions:

- the complete copied archive contains 2,443 files and about 2.1 GB and is not
  a deployable payload;
- the complete current Trøndelag UI requires exactly 45 reviewed files,
  101,938,537 uncompressed bytes;
- the corresponding raw subset from frozen commit `75ba148` was rejected
  because it produced 11.7%/9.2% rather than the accepted 6.7%/6.2%;
- nine reviewed runtime files differ from that frozen commit, so the new
  package is explicitly a V2 Final runtime payload, not an exact frozen-V2
  export;
- the deterministic ZIP is 15,730,706 bytes with SHA-256
  `43e6ccc8cae99c7a7e15f85d92a8e3e9c15a077abfb9b28bd4c12a92fc63202c`;
- the 45-file content aggregate is
  `f05045ac0a91fd0f83629d7507157ed62c504f1a95034339200c4e784d4212c2`;
- only the small package manifest, packager, bootstrap, and validators belong
  in Git. The ZIP, extracted cache, and full archive stay outside Git.

The root bootstrap now verifies HTTPS, exact archive bytes/checksum, safe ZIP
paths and file types, exact inventory, every inner checksum, resource caps,
and a digest-named cache before setting `SPEEDLOCAL_V2_SOURCE_ROOT`. The legacy
V2 manifest layer now consumes the same shared provider resolver as the
generic engine.

Validation:

- runtime bundle security and real-package validator: PASS, 25/25;
- real root `app.py` render from the extracted package: PASS;
- package Trøndelag runtime sources: PASS, 32/32;
- package accepted reviewed V2 Final baseline: PASS, 6/6;
- package interactive V2 Final AppTest: PASS, 20/20, including
  6.7% -> 6.2% and isolated `roads_large` 96.9% -> 95.5%;
- full-archive generic engine: PASS, 56/56;
- full-archive source adapter: PASS, 33/33;
- full-archive runtime summary: PASS, 23/23;
- frozen V2 local reference: PASS, 50/50;
- delivery repository after removing generated workspace artifacts: PASS,
  73/73;
- anonymous download of all three published Release assets: PASS; ZIP and
  manifest SHA-256 match the tracked contract;
- clean bootstrap with no `SPEEDLOCAL_V2_SOURCE_ROOT`, using the public Release
  asset: PASS;
- public-package interactive V2 Final AppTest: PASS, 20/20, including
  6.7% -> 6.2%;
- frozen V2 remote reference: PASS, 53/53;
- Windows cache promotion probe and clean public cold start after replacing
  directory `os.replace()` with atomic `os.rename()`: PASS;
- authenticated Streamlit Cloud visual and interaction smoke review: PASS;
  the user confirmed that the deployed Trøndelag app works correctly.

## Blockers

- The Bornholm V1 repository/checkpoint must be recovered and technically
  pinned before Bornholm behavioral parity work begins.

## Next starting point

Start canonical R7-to-R6/R5 aggregation for `roads_large`. Bornholm onboarding
does not block that increment.

## Work checkpoint

- Runtime transport commit:
  `d914563f49e95c65d13c79d6a66b08b4bc26392b`
  (`Provide verified Trondelag cloud runtime`).
- Current application commit:
  `72f1783d03166434ad5e35e1a8a85b27312d5e33`
  (`Fix Windows runtime cache promotion`).

## Publication

- Push: `main` pushed to `origin`; the remote branch resolves to the current
  application checkpoint `72f1783d03166434ad5e35e1a8a85b27312d5e33`.
- GitHub checkpoints: verified.
- Runtime prerelease:
  `https://github.com/gislab-se/speedlocal/releases/tag/v2-final-runtime-trondelag-r7-2026-07-30.1`
  (Release id `362553108`), attached to transport checkpoint `d914563`.
- Release assets: exactly three published assets, with ZIP size `15,730,706`
  bytes and SHA-256
  `43e6ccc8cae99c7a7e15f85d92a8e3e9c15a077abfb9b28bd4c12a92fc63202c`;
  anonymous redownload and checksum verification passed.
- Public-package cold start: passed without a local archive path, followed by
  the 20/20 interactive V2 Final gate.
- Frozen V2 remote validation: PASS, 53/53, including both protected branches
  and the protected tag at `75ba14871100c208cbf8eedb794d56c165340811`.
- V2 Final deployment endpoint: PASS. The app remains access-controlled, and
  the user completed the authenticated visual and interaction smoke review.
- Publication-record commit: this documentation-only follow-up to `a30fff9`;
  its hash is available directly in Git history.
- Final worktree: expected clean after the publication-record commit.
