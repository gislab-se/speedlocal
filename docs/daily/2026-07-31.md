# Daily plan — 2026-07-31

## Outcome

Deliver a coherent `v2-final-dev` checkpoint that completes canonical
Trøndelag roads calculation at R7/R6/R5: first finish and locally approve the
`roads_large`/verification increment, then migrate `roads_medium` and
medium-plus-large to one manifest-selected generic-engine request while
Frozen V2 cell parity remains unchanged.

## Starting state

- Branch and commit: `main`, `97c7c19`.
- Worktree: clean and aligned with `origin/main`.
- Active slice: complete public roads behavior.
- Previous handoff: `roads_large` runs through `speedlocal.run_analysis` at R7;
  R6/R5 and `roads_medium` still use the legacy calculation path.

### Continuation checkpoint

- The canonical R6/R5 implementation was committed on `v2-final-dev` as
  `9665990` before the cleanup increment began.
- `main` remained at the published commit `97c7c19`; this continuation does
  not use a publication window and must not update `main`.
- The user approved the clean-process localhost review of the empty start,
  `roads_large` R7/R6/R5, model-area copy, and map-only source/buffer controls.
  That prior increment is locally promoted but remains unpublished.
- The next increment starts from the clean pushed development checkpoint
  `56a4d6f` and targets `roads_medium` plus combined-road calculation only.

## Tasks

- [x] Complete and validate canonical `roads_large` R7/R6/R5 plus the empty
  start, map-review, and manifest-model-area verification cleanup.
- [x] Record the user's clean-process localhost approval and keep `main`
  unchanged outside a publication window.
- [x] Characterize frozen `roads_medium` and medium-plus-large behavior at
  300/1000 m for R7/R6/R5 with independent raw-distance oracles.
- [x] Route every selected road layer through one canonical generic-engine
  request and remove medium/combined fallthrough to legacy distance loading.
- [ ] Complete the final diff audit, create the coherent checkpoint commits,
  and push `v2-final-dev`.

## Expected files and data

- Expected code: `speedlocal/contracts.py`, `speedlocal/sources.py`,
  `speedlocal/engine.py`, and the narrow `apps/v2_port/` bridge/call site.
- Expected contracts: `regions/trondelag/analyses/wind.json`.
- Expected validators: generic engine, V2 Final baseline parity, and V2 Final
  AppTest.
- Expected documentation: this log, `docs/slices/roads.md`, and status wording
  in the authoritative/delivery plans if the checkpoint passes.
- Runtime data remains read-only under `SPEEDLOCAL_V2_SOURCE_ROOT`; no large
  artifact is copied into Git.

## Acceptance checks

- R7 remains unchanged at 13,735 cells and its approved 300/1000 m values.
- R6 contains exactly 2,163 display parents and R5 exactly 365.
- Every R6/R5 cell value matches an independent frozen-V2 rollup oracle.
- The real V2 Final result uses one canonical selected-roads path at all three
  public resolutions and fails closed on incomplete or inconsistent domains.
- Medium-only and medium-plus-large match every frozen-V2 cell at 300/1000 m
  for R7/R6/R5, including fixed aggregate, blocked-count, and model-area
  anchors.
- No selected road layer reaches the legacy distance loader; non-road groups
  remain on the explicit transitional adapter.
- Required repository, source, runtime, guardrail, and Streamlit validators
  pass.
- The unfiltered startup request shows 100% potential and no source/buffer
  toggles until a canonical layer is selected.
- Source and buffer toggles change map presentation only; selection,
  parameters, and the calculated result remain unchanged.
- The table uses per-cell `display_area_m2` from the analysis contract and
  labels the result as model area, not exact polygon-clipped land area.

## Known risks

- H3 parent derivation may require a runtime dependency or an already reviewed
  parent field; the implementation must not infer parents from region names.
- Display rollups may use weighted or minimum semantics that differ from a
  simple mean and therefore require exact characterization before coding.
- The public runtime ZIP may need a new manifest/release only if additional
  source files become required; avoid changing transport when existing
  reviewed files are sufficient.
- A module-boundary change requires a complete Streamlit process-tree restart
  before visual review.

## Decisions and discoveries

- Frozen V2 aggregates all 13,851 valid R7 distance rows to each H3 parent
  using minimum distance and any intersection, then filters to the public
  target domain. Filtering to the 13,735 public R7 cells first would change
  minimum distance in three R6 and two R5 public parents.
- The public R7 display cells derive exactly 2,163 R6 parents and 365 R5
  parents. Those reviewed display files now form manifest-declared rollup
  contracts rather than app-code path constants.
- The existing checksum-pinned 45-file cloud package already contains the R7
  distance table and both coarser display files. No runtime inventory, ZIP, or
  Release change is required for this checkpoint.
- H3 resolution is a discrete contract. Boolean, string, fractional,
  undeclared, upward, and mixed-resolution requests fail closed rather than
  being coerced.
- The Streamlit bridge delegates canonical-domain validation to
  `speedlocal.run_analysis`; it does not parse the large domain twice or
  select an algorithm from a region id.
- The delivery schedule moved one business day because the roads slice is not
  locally promoted until `roads_medium`, combined behavior, and obsolete-path
  removal pass.
- The publication cadence is now explicit: normal development checkpoints go
  to `v2-final-dev`; `main` is the published branch. Friday is the normal
  publication window and Tuesday is optional for a coherent locally promoted
  increment. An emergency outside those windows requires a recorded reason.
- Frozen medium roads are N500 category `F` (2,252 line features); large roads
  are categories `E`/`R` (327 features). Both use the same 13,851-cell R7
  distance universe and shared soft-distance slider.
- The pinned accepted V2 Final parity and manifest default is 300 m. The old
  registry also declares a conflicting 100 m fallback; it must not silently
  redefine the accepted checkpoint.
- Medium-plus-large is one group calculation: minimum selected-layer distance,
  logical-OR intersection, then the shared soft ramp. The current hybrid and
  generic engine were cellwise identical before the old path was removed.
- The reviewed 45-file runtime package already contains all medium-road source
  and distance assets. Runtime inventory, ZIP, and Release remain unchanged.

## Code classification

- Keep: Frozen V2's unweighted cell mean as the explicit parity metric; the
  registry adapter for non-road slices; solar's unchanged legacy area path;
  the shared Streamlit form/session model; and downstream map/table consumers.
- Extract: generic H3 distance rollup, manifest-domain area resolution,
  provider-resolved metric vector buffering, and one selected-roads result
  adapter over `speedlocal.run_analysis`.
- Configure: R6/R5 paths, ids, resolutions, counts, `display_area_m2`, and the
  empty default request, available road ids, and shared road buffer contract in
  the wind manifests.
- Rewrite: the road branch in `_wind_fast_distance_runtime_result` so the
  complete selected road set is one canonical request; retain the narrow
  `transport` boundary until its UI/data adapter increment.
- Remove: `MIGRATED_ROADS_LAYER_ID`, the large-only bridge, and every road
  fallthrough to `distance_table_for_layer`. Shared legacy distance helpers
  remain only for non-road slices.

## Completed

- Created the start-of-day plan from the authoritative V2 Final and delivery
  plans.
- Added manifest and validation contracts for the exact R6/R5 analysis
  domains.
- Added raw-first H3 parent aggregation with minimum-distance and
  any-intersection composition.
- Connected the actual V2 Final `roads_large` flow to the generic result at
  R7/R6/R5 while preserving the legacy `roads_medium` composition.
- Added independent frozen-V2 cellwise oracles, exact aggregate anchors,
  malformed-resolution checks, and real Streamlit interaction checks at both
  300 and 1000 m.
- Updated the authoritative, delivery, slice, and agent instructions to make
  this checkpoint and the next boundary explicit.
- Added manifest-declared empty startup requests for both wind contracts and
  connected Trøndelag's request to the actual V2 Final state initialization.
- Limited the public wind and solar controls to the five groups whitelisted by
  the wind manifest; obsolete public military, aviation, bird, coast,
  land-use, and reindeer sections are no longer rendered. Outside canonical
  roads, layer options and readiness remain a transitional registry adapter.
- Added separate map-review controls and a generic EPSG:25832 vector-buffer
  preview for `roads_large`, `roads_medium`, and their combination.
- Added the generic `display_area_m2` resolver and R7/R6/R5 rollup validation.
  Trøndelag now uses model area from the manifest in the wind table,
  establishment score, coarser rollups, class dominance, and scenario
  allocation while the Frozen V2 unweighted parity percentages remain
  unchanged.
- Relabelled the retained Frozen V2 percentage as mean potential per analysis
  cell so it is not mistaken for physical or model-area-weighted land share.
- Recorded the user's clean-process localhost approval of the preceding
  `roads_large` and verification-cleanup increment.
- Generalized the bridge from one hardcoded large-road layer to the
  manifest-ordered selected road set, with fail-closed empty, duplicate,
  undeclared-layer, and domain checks.
- Replaced the hybrid canonical-large/legacy-medium branch with one canonical
  group request. Medium-only, large-only, and combined roads now share the same
  actual V2 Final calculation path.
- Added independent frozen-CSV cell oracles and fixed R7/R6/R5 aggregate,
  blocked-count, model-area, endpoint, order-invariance, and real-Streamlit
  checks for medium-only and combined roads.

## Validation

- Generic engine: PASS, 325/325, including every medium-only, large-only, and
  combined R7/R6/R5 `hex_id → acceptance` value at 300/1000 m, endpoint
  monotonicity, order invariance, and the raw-first R5 guard cell.
- Frozen-V2 V2 Final parity: PASS, 46/46, including unchanged full-flow and
  large-road evidence plus exact medium/combined cell, blocked-count, and
  model-area anchors, raw-selection fail-closed checks, and legacy road loading
  disabled at R7/R6/R5.
- Vector-buffer preview: PASS, 14/14, including large, medium, combined, CRS,
  serialization, and fail-closed checks.
- Real Streamlit AppTest: PASS, 41/41, including empty manifest startup,
  five-group visibility, map-only review toggles, and medium-only, large-only,
  and combined R7/R6/R5 anchors at 300/1000 m.
- Runtime bundle security: PASS, 20/20; package inventory was unchanged.
- V2 port guardrails: PASS, 17/17.
- Delivery repository: PASS, 82/82.
- Region readiness: PASS, 36/36.
- Frozen V2 local reference: PASS, 50/50.
- V2 source adapter: PASS, 33/33.
- File runtime summary: PASS, 23/23.
- Bornholm V2 diagnostics: PASS, 6/6; diagnostic integrity only, not product
  parity or readiness.
- Trøndelag runtime sources: PASS, 32/32.
- Compileall, dependency check, and `git diff --check`: PASS.
- One clean V2 Final server was started through `scripts/start_app.ps1`; exactly
  one listener serves `127.0.0.1:8502`, and `/_stcore/health` returns HTTP 200
  with `ok`.
- Pre-existing Streamlit bare-mode, session-state, and
  `st.components.v1.html` deprecation warnings remain outside this slice; no
  validator reported a blocker.

## Blockers

- No code or data blocker for the automated checkpoint.
- The previous `roads_large`/verification increment is locally approved.
  External publication remains a separate state and waits for an eligible
  Tuesday or Friday window.
- Bornholm V1 technical pinning remains a later onboarding blocker, not a
  Trøndelag roads blocker.
- The new medium/combined increment has no automated blocker. Human localhost
  review and removal of the temporary legacy `transport` UI/data adapter still
  remain before the complete roads slice can be locally promoted.

## Next starting point

Review one clean V2 Final process at `http://127.0.0.1:8502/?region=trondelag`.
Select only `Mellanvägar`, then both `Mellanvägar` and `Stora vägar`; compare
300/1000 m at R7/R6/R5 against the visible anchors in
`docs/slices/roads.md`, and verify source/buffer remain map-only. After human
approval, move road labels, readiness, and source display from the legacy
registry to manifest descriptors and remove the temporary `transport` UI/data
adapter. Publish only an exact eligible checkpoint in a later Tuesday or
Friday window.

## Work checkpoint

- Branch: `v2-final-dev`.
- Commit: pending final checkpoint creation.
- Push: pending.
- GitHub development checkpoint: pending.

## Local promotion

- Status: preceding `roads_large` and verification-cleanup increment locally
  promoted; current medium/combined increment not yet locally promoted.
- Scope: locally approved scope is empty manifest start, public group cleanup,
  road map review, manifest model area, and canonical `roads_large` R7/R6/R5.
  Current automated scope adds canonical `roads_medium` and combined roads.
- Automated evidence: PASS as recorded above.
- Localhost visual evidence: user approved the preceding scope in this
  conversation on 2026-07-31; medium/combined visual review is pending.

## Publication

- Window: not used for this medium/combined checkpoint.
- Emergency reason, if outside Tuesday or Friday: not applicable.
- Published commit on `main`: not created.
- Existing `main` remained unchanged at
  `97c7c19051b153787ffeb8dd1ab060ca0d14c117`.
- GitHub published checkpoint: not created.
- V2 Final deployment: existing deployment unchanged.
- Runtime release, if applicable: not applicable; runtime inventory and
  Release remain unchanged.
- Accepted-reference comparison: automated local comparison PASS; external
  comparison pending a publication window.
- GitHub Pages, if applicable: not applicable; `site/**` is unchanged.
- Publication-record commit: not created.
- Final worktree: pending final checkpoint and daily-record commits;
  medium/combined localhost visual approval remains a separate local-promotion
  gate.
